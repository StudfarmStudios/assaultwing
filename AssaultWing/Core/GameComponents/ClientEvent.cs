using AW2.Core.OverlayComponents;

// TODO: Peter: Add more diverse LogicExternalEvent types and possibly reduce the ProgramLogic interface?
public struct ClientEvent {
    // Now only one event, ShowDialog
    public OverlayDialogData dialogData;
}